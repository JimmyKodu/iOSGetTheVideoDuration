# iOSGetTheVideoDuration

静态页面（HTML + JS），用于在 iOS 浏览器中获取本地选取视频（iOS 可能转成 `.mov`）的时长。页面会依次尝试 5 种获取方案，任一方案超时会自动切换下一种，并实时渲染进度。

使用方式：

1. 打开 `index.html`（直接双击或通过本地服务器访问）。
2. 选择视频文件并点击“开始获取”。
3. 在页面中查看各方案的尝试结果和最终时长。
